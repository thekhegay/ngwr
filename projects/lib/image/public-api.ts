export { WrImage } from './image';
// `WrLightbox` is the canonical class name going forward — both names
// refer to the same component (component also accepts `<wr-image>` or
// `<wr-lightbox>` selectors).
export { WrImage as WrLightbox } from './image';
