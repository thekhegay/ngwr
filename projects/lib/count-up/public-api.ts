/**
 * @deprecated The `ngwr/count-up` entry will be removed in v9. Import
 * `WrCountUp` from `ngwr/counter` instead — same component, just
 * consolidated alongside `<wr-counter>` and its types.
 */
export { WrCountUp, type WrCountUpDirection, type WrCountUpEasing, type WrCountUpTrigger } from './count-up';
