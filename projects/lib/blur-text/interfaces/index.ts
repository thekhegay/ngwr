/** Granularity of the split. */
type WrBlurTextUnit = 'chars' | 'words';

/** Entry direction. `'top'` slides down into place; `'bottom'` slides up. */
type WrBlurTextDirection = 'top' | 'bottom';

export type { WrBlurTextDirection, WrBlurTextUnit };
