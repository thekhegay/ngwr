/**
 * @deprecated The `ngwr/tag` entry will be removed in v9. Import `WrTag`
 * from `ngwr/badge` instead — same component, just consolidated
 * alongside `<wr-badge>` and its types.
 */
export { WrTag } from './tag';
export type { WrTagIconPosition } from './types';
