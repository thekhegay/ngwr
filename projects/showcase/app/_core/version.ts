/**
 * Library version shown in the docs footer.
 *
 * Kept in sync with `projects/lib/package.json` manually for now.
 * TODO: replace with a build-time inject from package.json when the
 * Angular CLI lands stable JSON imports.
 */
export const NGWR_VERSION = '6.1.1';
