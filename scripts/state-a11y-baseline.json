{
  "color-contrast (light) — date-picker/calendar": {
    "targets": [
      ".wr-calendar__day--out-of-month.wr-calendar__day[role=\"gridcell\"]"
    ],
    "note": "2.23:1, measured 2026-08-14. A design call, not a patch, and NOT the WCAG exemption the route baseline claims for it: out-of-month days are enabled `<button>`s (`disabled=false`, `aria-disabled=false`) reachable with the arrow keys, so 1.4.3 applies to them. The colour is right — `--wr-calendar-muted` is `#5f6c7d`, which clears AA on white at 4.63:1 — and `opacity: 0.55` is what drops it to 2.23. Raising the opacity does not fix it either: at 0.75 the composite is #87919e (3.2:1), and reaching 4.5 needs essentially the full-strength muted colour, which is the same tone as an in-month day. So the de-emphasis and the ratio cannot both be had by tuning this one number — it wants a different signal (weight, a dot, a divider) or non-actionable out-of-month days."
  },
  "color-contrast (dark) — date-picker/calendar": {
    "targets": [
      ".wr-calendar__day--out-of-month.wr-calendar__day[role=\"gridcell\"]"
    ],
    "note": "3.11:1, the same element and the same design call as the light entry — #5a6374 on #0b1120. Dark starts closer because the muted role lightens, and the 0.55 opacity is still what puts it under."
  },
  "color-contrast (light) — calendar/today-and-selected": {
    "targets": [
      ".wr-calendar__day--out-of-month.wr-calendar__day[role=\"gridcell\"]"
    ],
    "note": "The same out-of-month cells as the date-picker entry, reached through `wr-calendar` directly rather than through a picker's popup — 2.23:1, 17 nodes across the page's three calendars. One design call, two states that paint it."
  },
  "color-contrast (dark) — calendar/today-and-selected": {
    "targets": [
      ".wr-calendar__day--out-of-month.wr-calendar__day[role=\"gridcell\"]"
    ],
    "note": "3.11:1, the dark half of the entry above."
  },
  "target-size (light) — window/open": {
    "targets": [
      ".wr-window__chrome-action--close",
      ".wr-window__chrome-action--minimize",
      ".wr-window__chrome-action--maximize"
    ],
    "note": "14×14px where WCAG 2.5.8 wants 24, found the first time anything measured an OPEN window — `check:contrast` walks pages at rest, where the chrome does not exist. A design call in the same family as the carousel dots, and the reason it is not a quiet patch: these are traffic-light buttons, and 24px ones are a different title bar. There IS a fix that keeps the look — give the button a 24px box and paint the 14px dot with a `::before` — but it changes the chrome's spacing, so it is the maintainer's call rather than a gate's."
  },
  "target-size (dark) — window/open": {
    "targets": [
      ".wr-window__chrome-action--close",
      ".wr-window__chrome-action--minimize",
      ".wr-window__chrome-action--maximize"
    ],
    "note": "The same three buttons at the same 14×14px — the chrome is not themed."
  },
  "target-size (light) — carousel/dot-active": {
    "targets": [
      ".wr-carousel__dot--active",
      "button[aria-label=\"Go to slide 2\"]",
      "button[aria-label=\"Go to slide 3\"]",
      "button[aria-label=\"Go to slide 4\"]"
    ],
    "note": "8×8px dots (20×8 for the active one) where WCAG 2.5.8 wants 24, and centres 14px apart, so the undersized-target spacing exception does not rescue them either. Already recorded in `contrast-baseline.json` as a design call — it appears here too because hovering a dot is a state, and the rule fires on the element either way. Fix them in one place and both entries go."
  },
  "target-size (dark) — carousel/dot-active": {
    "targets": [
      ".wr-carousel__dot--active",
      "button[aria-label=\"Go to slide 2\"]",
      "button[aria-label=\"Go to slide 3\"]",
      "button[aria-label=\"Go to slide 4\"]"
    ],
    "note": "The same dots at the same size — not themed."
  },
  "target-size (light) — event-calendar/month": {
    "targets": [
      ".wr-event-calendar__chip--band.wr-event-calendar__chip",
      ".wr-event-calendar__chip--band.wr-event-calendar__chip[aria-label=\"Standup, 09:30 AM\"]",
      ".wr-event-calendar__chip--band.wr-event-calendar__chip[aria-label=\"Release window, 11:00 AM\"]",
      ".wr-event-calendar__more"
    ],
    "note": "20px-tall event chips where WCAG 2.5.8 wants 24, already recorded in `contrast-baseline.json` as a design call: raising them fits one fewer event per month cell before the overflow indicator appears, which is a density decision about the component. Stacked chips also sit closer than 24px apart, so the spacing exception does not rescue them. The `aria-label`s are stable only because the sweep freezes the clock — the demo builds its events from `new Date()`, and two nightly runs went red on this same finding wearing new titles before that landed. `.wr-event-calendar__more` — the \"+N more\" button — joined the entry on 2026-09-01 at 64.6×18px: wide enough and 6px short, so it is the height that fails. It is the SAME decision as the chips rather than a second one, because it is the overflow indicator the sentence above is about — raising only it makes the summary taller than the 20px chips it summarises and grows the day cell with it, and raising both is exactly the density change already declined. The `touch-target` mixin is not an escape here: it grows the hit area with an `::after`, and axe measures the element's own client rects, so a pseudo-element does not move this number — and it sits behind `@media (pointer: coarse)`, which the desktop context (`hasTouch: false`) never matches anyway. It had failed unbaselined on every nightly since 2026-08-21."
  },
  "target-size (dark) — event-calendar/month": {
    "targets": [
      ".wr-event-calendar__chip--band.wr-event-calendar__chip",
      ".wr-event-calendar__chip--band.wr-event-calendar__chip[aria-label=\"Standup, 09:30 AM\"]",
      ".wr-event-calendar__chip--band.wr-event-calendar__chip[aria-label=\"Release window, 11:00 AM\"]",
      ".wr-event-calendar__more"
    ],
    "note": "The same chips and the same 64.6×18px \"+N more\" button as the light entry — none of it is themed. The two findings the nightly reported are one element counted once per theme, so the button is baselined here for the same reason it is there: it is the overflow indicator the chips' density decision is about, raising it alone would make it taller than the chips it summarises, and `touch-target`'s `::after` cannot help because axe measures the element's own client rects."
  }
}
